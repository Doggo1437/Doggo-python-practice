chain1 = "hola, doggo, como, esta?"
chain2 = "saludos doggo"

result = len(chain1)



#replace data ([data to be replaced],{replacement data})
new_chain = chain1.replace("hola","hello")

#split chains with chain given

split_chain = chain1.split(",")
print(split_chain[0])

